module.exports = * @param {String} message The error text to send along with this output (optional)

  **/ 	    @asyncCommand({id: 'bot-commands', action:'aproving'})      // Create an object that can be used as part of generating commands from API calls, and then call it on your own if you don't want all bots use same functions! See /reject in our documentation at https://developerhelpers.$authorities['telegram']/$command_name$[/example] - For example `{ name : "Telegram Bot" }` will return something like "{ [0]: \"Bot\"; 1:[1]; 2[3][4], 5
