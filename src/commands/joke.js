module.exports = The output is in JSON format and will be sent as plain text to all users of this server, but it may contain more information than just that specified by an optional second parameter (a map).
func Joke(bot *Bot) {

	// Call /talk:command with parameters "text" - sends user's joke message out via websocket using "/wspy/<message>" protocol over which we can send json-format messages back from here!
}
